package com.lvc.loginsimulator.business

/**
 * Can be an alternative to have this extra layer instead of calling repository directly on ViewModel.
 * Since its a simple project, this will be avoid to keep the project short and simple.
 *
 */
class SignInUseCase {
}